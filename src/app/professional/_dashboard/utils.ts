/**
 * utils.ts — Dashboard Professionnel
 * Derviche Diffusion
 *
 * Fonctions utilitaires partagées entre les cartes du dashboard pro.
 */

import {
  formatDateFr as formatDateLong,
  formatDateShort,
  formatTimeShort as formatTime,
} from '@/lib/utils/format-date';

export { formatDateLong, formatDateShort, formatTime };
